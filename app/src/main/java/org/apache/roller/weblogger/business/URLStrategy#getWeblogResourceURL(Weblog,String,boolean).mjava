    /**
     * Get url to a resource on a given weblog.
     */
    String getWeblogResourceURL(Weblog weblog,
                                                    String filePath,
                                                    boolean absolute);

