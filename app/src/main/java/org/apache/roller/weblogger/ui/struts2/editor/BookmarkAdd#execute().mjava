    @SkipValidation
    public String execute() {
        
        if(getFolder() == null) {
            addError("Cannot add bookmark to null folder");
            return ERROR;
        }
        
        return INPUT;
    }

