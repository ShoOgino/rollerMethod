    public WeblogBookmarkFolder(
            String name,
            String desc,
            Weblog weblog) {
        
        this.name = name;
        this.description = desc;
        this.weblog = weblog;
        weblog.addBookmarkFolder(this);
    }

