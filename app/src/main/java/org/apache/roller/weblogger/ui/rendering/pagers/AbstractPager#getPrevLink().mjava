    public String getPrevLink() {
        if (page > 0) {
            int prevPage = page - 1;
            Map params = new HashMap();
            params.put("page", ""+prevPage);
            return createURL(url, params);
        }
        return null;
    }

