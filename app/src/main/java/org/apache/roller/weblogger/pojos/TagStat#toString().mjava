    public String toString() {
        StringBuilder str = new StringBuilder("{");

        str.append("name=" + name + " " + "count=" + count);
        str.append('}');

        return (str.toString());
    }

