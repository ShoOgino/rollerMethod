    /**
     * Get root url for a given weblog.  Optionally for a certain locale.
     */
    String getWeblogURL(Weblog weblog,
                                            String locale,
                                            boolean absolute);

