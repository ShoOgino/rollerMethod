    private boolean isInstallUrl(String uri) {
        return (uri != null && (
                   uri.endsWith("create.rol") 
                || uri.endsWith("bootstrap.rol") 
                || uri.endsWith(".js") 
                || uri.endsWith(".css")));
    }

