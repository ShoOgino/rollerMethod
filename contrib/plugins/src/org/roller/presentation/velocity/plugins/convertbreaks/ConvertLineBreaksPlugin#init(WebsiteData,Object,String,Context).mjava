    public void init(
            WebsiteData website,
            Object config,
            String baseURL,
            Context ctx) throws RollerException {
        // we don't need to do any init.
        mLogger.debug("initing");
    }

