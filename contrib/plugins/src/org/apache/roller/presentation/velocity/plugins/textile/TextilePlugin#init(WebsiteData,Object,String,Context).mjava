    /** 
     * Put plugin into the page context so templates may access it.
     */
    public void init(
            WebsiteData website,
            Object config,
            String baseURL,
            Context ctx)
    {
        ctx.put("textileRenderer",this);
    }

