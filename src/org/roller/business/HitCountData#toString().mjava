   public String toString()
   {
      StringBuffer str = new StringBuffer("{");

      str.append("id=" + id + " " + "dayHitCount=" + dayHitCount);
      str.append('}');

      return(str.toString());
   }

