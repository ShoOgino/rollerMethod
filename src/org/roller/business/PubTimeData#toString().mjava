   public String toString()
   {
      StringBuffer str = new StringBuffer("{");

      str.append("id=" + id + " " + "pubTime=" + pubTime);
      str.append('}');

      return(str.toString());
   }

