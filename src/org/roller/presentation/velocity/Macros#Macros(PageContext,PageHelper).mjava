    //------------------------------------------------------------------------
    /** Construts a macros object for a JSP page context. */
    public Macros( PageContext ctx, PageHelper helper )
    {
        mPageContext = ctx;
        mPageHelper = helper;
    }

