    public FolderAssoc(
        String id,
        FolderData folder,
        FolderData ancestor,
        String relation)
    {
        this.id = id;
        this.folder = folder;
        this.ancestor = ancestor;
        this.relation = relation;
    }

