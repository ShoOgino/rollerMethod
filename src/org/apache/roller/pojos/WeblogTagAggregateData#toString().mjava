   public String toString() {
     StringBuffer str = new StringBuffer("{");
     
     str.append("id=" + id + " " +
             "websiteid=" + website.getId() +
             "name=" + name + " " +
             "count=" + count);
     str.append('}');
     
     return (str.toString());
 }

