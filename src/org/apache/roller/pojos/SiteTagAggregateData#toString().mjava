   public String toString() {
     StringBuffer str = new StringBuffer("{");
     
     str.append("id=" + id + " " +
             "name=" + name + " " +
             "count=" + count);
     str.append('}');
     
     return (str.toString());
 }

