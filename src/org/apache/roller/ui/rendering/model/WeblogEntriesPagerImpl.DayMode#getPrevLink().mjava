        public String getPrevLink() {
            String ret = null;
            if (page > 0) {
                ret = weblog.getURL() + "/date/" + dateString + queryString(-1);
            }
            return ret;
        }

