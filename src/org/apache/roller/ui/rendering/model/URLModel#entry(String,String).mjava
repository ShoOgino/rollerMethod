    public String entry(String anchor, String catPath) {
        String ret = weblog.getURL()+"/entry/"+anchor;
        if (catPath != null) {
            ret += "?cat="+catPath;
        }
        return ret;
    }

