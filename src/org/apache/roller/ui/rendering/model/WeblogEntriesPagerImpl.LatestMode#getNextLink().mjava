        public String getNextLink() {
            if (more) {
                return createURL(page, 1, weblog, weblogPage, entry, cat, dateString, locale);              
            }
            return null;
        }

