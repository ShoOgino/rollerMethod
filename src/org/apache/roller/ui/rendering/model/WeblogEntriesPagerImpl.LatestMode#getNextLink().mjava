        public String getNextLink() {
            String ret = null;
            if (more) {
                ret = weblog.getURL() + queryString(1);                
            }
            return ret;
        }

