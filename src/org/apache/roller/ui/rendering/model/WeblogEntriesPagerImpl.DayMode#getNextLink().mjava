        public String getNextLink() {
            String ret = null;
            if (more) {
                ret = weblog.getURL() + "/date/" + dateString  + queryString(1);
            }
            return ret;
        }

