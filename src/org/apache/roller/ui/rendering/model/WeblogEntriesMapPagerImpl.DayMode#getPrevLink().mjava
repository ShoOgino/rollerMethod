        public String getPrevLink() {
            if (page > 0) {
                return createURL(page, -1, weblog, weblogPage, null, cat, dateString, locale);
            }
            return null;
        }

