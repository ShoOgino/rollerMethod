    public String toString() {
        StringBuffer str = new StringBuffer("{");

        str.append("name=" + name + " " + "count=" + count);
        str.append('}');

        return (str.toString());
    }

