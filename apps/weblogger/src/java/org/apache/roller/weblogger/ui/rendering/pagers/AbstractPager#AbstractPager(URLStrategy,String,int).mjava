    public AbstractPager(URLStrategy strat, String baseUrl, int pageNum) {
        
        this.urlStrategy = strat;
        this.url = baseUrl;
        if(pageNum > 0) {
            this.page = pageNum;
        }
    }

