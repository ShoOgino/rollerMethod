    public StartupException(String msg, List<String> log) {
        super(msg);
        
        if(log != null) {
            this.startupLog = log;
        } else {
            this.startupLog = Collections.EMPTY_LIST;
        }
    }

