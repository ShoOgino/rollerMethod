    /**
     * Show stylesheet edit page.
     */
    public String execute() {
        
        if(getTemplate() == null) {
            return ERROR;
        }
        
        setContents(getTemplate().getContents());
        
        return INPUT;
    }

