    /**
     * True if permission specifies no actions
     */
    public boolean isEmpty() {
        if (actions == null || actions.trim().length() == 0) {
            return true;
        }
        return false;
    }

