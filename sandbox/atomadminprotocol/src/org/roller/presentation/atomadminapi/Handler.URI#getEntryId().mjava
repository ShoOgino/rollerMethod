        public String getEntryId() {
            if (getEntryIds() == null) {
                return null;
            }
            if (getEntryIds().length == 0) {
                return null;
            }
            return getEntryIds()[0];
        }

